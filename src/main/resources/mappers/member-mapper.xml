<<<<<<< HEAD
<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE mapper PUBLIC
"-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="memberMapper">

	<resultMap id="memberResultMap" type="Member"></resultMap>
	<select id="login" resultMap="memberResultMap" parameterType="Member"
		resultType="Member">
		SELECT * FROM MEMBER WHERE
		ID = #{id} AND PASSWORD = #{password}
	</select>

	<insert id="enroll">
		insert into member
		values (MEM_SEQ.NEXTVAL,#{id},#{password},#{nickname},#{email},null,0,0,0,0,#{job},SYSDATE,null)
	</insert>
=======
<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE mapper PUBLIC
"-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="memberMapper">

	<select id="login" parameterType="Member"
		resultType="Member">
		SELECT * FROM MEMBER WHERE ID = #{id} AND PASSWORD = #{password}
	</select>

	<insert id="enroll">
		insert into member
		values (MEM_SEQ.NEXTVAL,#{id},#{password},#{nickname},#{email},null,0,0,0,0,#{job},SYSDATE,null)
	</insert>
	
	<update id="update">
		update member set
		PASSWORD=#{password},
		NICKNAME=#{nickname},
		EMAIL=#{email},
		
	</update>
>>>>>>> branch 'master' of https://github.com/Magenta9479/Hmm.git
</mapper>